class Solution {
public:
    // int gcd(int a, int b) {
    //     if (b == 0)
    //         return a;
    //     return gcd(b, a % b);
    // }

    int gcdOfOddEvenSums(int n) {
        // int sumo = 0, sume = 0;

        // for (int i = 1; i <= n; i++) {
        //     sumo += (2 * i - 1); // 1,3,5,...
        //     sume += (2 * i);     // 2,4,6,...
        // }

        // return gcd(sumo, sume);
        return n;
    }
};